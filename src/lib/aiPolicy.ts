/**
 * Политика ИИ-блока «Спроси ИИ».
 *
 * Здесь только константы — модуль без зависимостей, поэтому его безопасно
 * импортировать и на клиенте, и на сервере.
 *
 * Почему есть лимит: вызов внешней LLM — платный и недетерминированный ресурс.
 * Мы осознанно ограничиваем его 5 обращениями в сутки на браузер (день считается
 * по времени Астаны, UTC+5). Сверх лимита сервис не «ломается»: отвечает
 * встроенный офлайн-консультант Qadam, который работает по тому же профилю.
 */

/**
 * Сколько вопросов к «Спроси ИИ» в сутки получает один посетитель
 * (переопределяется переменной окружения AI_DAILY_LIMIT).
 *
 * Считаем именно вопросы, а не только вызовы внешней модели: так политика
 * одинакова и с ключом, и без него, а пользователь всегда видит один честный
 * счётчик. Когда лимит исчерпан, отвечает встроенный консультант — сервис
 * остаётся рабочим.
 */
export const AI_DAILY_LIMIT = 5;

/**
 * Версия текста согласия на передачу данных профиля в ИИ.
 * Меняйте при изменении состава передаваемых данных — тогда согласие
 * будет запрошено заново у всех пользователей.
 */
export const AI_CONSENT_VERSION = "2026-09-19";
